DB_SERVER=localhost
# Optional: set if using a named instance, e.g. SQLEXPRESS
DB_INSTANCE=
DB_PORT=1433
DB_NAME=Zonesoft
DB_USER=zonesoft_app
DB_PASSWORD=change-me
# Accept self-signed; set to false if you have proper certs
DB_TRUST_SERVER_CERT=true
# Comma-separated list of tables allowed for writes
ALLOWED_TABLES=Products,MenuItems,Orders

# Optional API settings (for hosted/API-only mode)
ZS_API_BASE=https://api.zonesoft.org
ZS_API_KEY=

